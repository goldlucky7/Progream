# 🎁 클로드 숨은 기능 사전

> 최종 갱신: 2026-09-18
> 출처: [Claude Code 주간 다이제스트](https://code.claude.com/docs/en/whats-new) · [전체 변경 이력(체인지로그)](https://code.claude.com/docs/en/changelog) (공식)

읽는 법: **명령어**는 대화창에 슬래시(`/`)로 시작해 그냥 치면 되고,
**터미널 명령어**는 CLI(터미널 앱)에서 실행하는 것입니다.
웹(claude.ai/code)에서는 터미널 전용 기능이 일부 동작하지 않습니다.

---

## 1. 잘 안 알려진 꿀 명령어

### `/usage` — 내 사용량 범인 찾기
플랜 한도가 뭐 때문에 닳는지 **스킬·서브에이전트·플러그인·MCP 서버별로** 쪼개서 보여줍니다.
"왜 이렇게 빨리 한도가 차지?" 싶을 때 제일 먼저 쳐보세요.
```
/usage
```

**🆕 이제 "Loops" 항목이 따로 생겼습니다**
`/loop`로 돌려둔 반복 작업과 예약 작업(루틴)이 **각각 한도를 얼마나 먹었는지** 보여줍니다.
- 몇 번 돌았는지 / 총 토큰 / **1회당 토큰** / 마지막 실행 시각
자동 실행을 여러 개 걸어둔 사람에게는 사실상 "누가 내 한도 다 썼나" 명세서입니다.
1회당 토큰이 유난히 큰 루틴이 보이면 그 루틴 프롬프트만 줄이면 됩니다.
> (Pro·Max·Team·Enterprise 플랜 / 2026-08-28 추가)

### `/rewind` — 대화 되감기 (생명의 은인)
대화를 과거 시점으로 되돌립니다. **`/clear`로 날려버린 대화도 복구**됩니다.
```
/rewind
```
> 팁: Rewind 메뉴 안의 **"Summarize up to here"**를 쓰면
> 앞부분 대화를 요약본으로 압축해서 컨텍스트를 확보할 수 있습니다.

### `/goal` — 완료 조건 걸어두기
"이 조건이 만족될 때까지 계속해"라고 목표를 박아두면, 여러 턴에 걸쳐 알아서 계속합니다.
```
/goal 대본이 공백 포함 15,000자를 넘을 때까지 계속 보강해
```

**🆕 이제 `/goal`이 중간에 사라지지 않습니다**
긴 대본 작업은 대화가 길어지면 자동으로 압축(요약)되는데,
예전엔 그때 걸어둔 목표가 **조용히 풀려버려서** 클로드가 "다 됐다"며 멈추곤 했습니다.
이제는 압축을 거치거나 세션을 나중에 이어받아도(`/resume`) **목표가 그대로 유지**됩니다.
> (2026-09-17 반영)

### `/loop` — 반복 자동 실행
같은 작업을 주기적으로 반복시킵니다. **시간 간격을 안 적으면** 클로드가 알아서 페이스를 조절합니다.
```
/loop 5m /code-review     ← 5분마다
/loop 팩트체크 다시 돌려줘   ← 클로드가 알아서 간격 결정
```

### `/fork` — 대화 복제해서 딴 길로
지금 대화를 그대로 복사해 **백그라운드 세션**으로 분리합니다.
원본은 안 건드리고 다른 방향을 실험할 때.
```
/fork
```

### `/doctor` (= `/checkup`) — 설정 종합 검진
설정 문제를 진단하고 **고칠 수 있는 건 바로 고쳐줍니다**. 뭔가 이상하면 이거부터.
```
/doctor
```

### `/cd` — 대화 중간에 폴더 갈아타기
프롬프트 캐시를 안 깨고 작업 디렉토리를 옮깁니다.
```
/cd ~/projects/other-project
```

### `/effort` — 사고 강도 슬라이더
클로드가 얼마나 깊게 생각할지 조절합니다. `xhigh`가 가장 높고, 어려운 작업에 권장됩니다.
```
/effort xhigh
```

> 🆕 이제 **모델별로 따로 기억**합니다. Opus는 높게, 가벼운 모델은 낮게 각각 저장됩니다.
> 목록에서 `Enter` 대신 **`s`** 를 누르면 "이번 세션만" 적용되고 기본값은 안 건드립니다.
> 항상 특정 단계 이상으로 안 올라가게 상한을 박아두려면 설정에 `maxEffortLevel`을 넣으면 됩니다.

### `/focus` — 화면 조용하게 만들기 🆕
클로드가 작업하는 동안 쏟아지는 중간 과정을 **다 접어버리고**,
내 질문 + 한 줄 요약 + 최종 답변만 보여줍니다. 한 번 켜두면 다음 세션에도 계속 유지되고,
다시 `/focus`를 치면 꺼집니다.
```
/focus
```
> (2026-09-11 추가. 전체 화면 렌더링 모드에서 동작)

### `/skill-doctor` — 안 쓰는 스킬 찾아내기 🆕
설치해둔 스킬 중 **실제로 안 쓰이는 것**과, 각 스킬이 잡아먹는 **컨텍스트 용량**을 리포트로 뽑아줍니다.
대본 스킬을 여러 개 깔아둔 경우, 뭘 빼야 대화 용량이 넉넉해지는지 바로 보입니다.
```
/skill-doctor
```
> (2026-09-04 추가. 대화형 세션에서만 동작)

### `/fast` — 이제 웹·루틴에서도 "빠른 모드" 🆕
같은 모델을 그대로 쓰면서 **답이 나오는 속도만 빨라지는** 모드입니다. (성능이 낮은 모델로 바뀌는 게 아닙니다.)
원래 내 컴퓨터 터미널에서만 됐는데, 이제 **claude.ai/code 웹 세션과 루틴(자동 실행)에서도** 켤 수 있습니다.
```
/fast        ← 켜기 / 끄기 토글
/fast off    ← 끄기
```
> (2026-09-14 추가. 회사·조직 계정은 관리자가 막아둔 경우 안 될 수 있습니다)

### 🆕 긴 대본 작업에서 대화가 너무 빨리 압축되던 문제 해결
클로드는 대화가 길어지면 알아서 앞부분을 요약해 압축(auto-compact)하는데,
내부 계산이 잘못돼서 **실제 한도의 절반 지점에서 미리 압축이 터지던 문제**가 고쳐졌습니다.
긴 대본을 한 세션에서 계속 붙들고 갈 때 전보다 훨씬 오래 원문 그대로 유지됩니다.
> (2026-09-15 반영)

### `/feedback` — 클로드가 대신 써주는 "문제 신고" 🆕
도구가 계속 실패하거나, 클로드가 요청을 못 해내거나, 내가 "그거 틀렸어"라고 지적하면
**클로드가 알아서 신고서 초안을 써서** 프롬프트 위에 카드로 올려둡니다.
내가 [보내기]를 누르기 전까지는 **아무것도 전송되지 않습니다.**
```
/feedback        ← 그동안 쌓인 초안 목록 열기 (다른 세션 것까지 전부)
```
초안을 골라서 고치거나, 보내거나, 버리면 됩니다.
초안 쓰는 것 자체가 귀찮으면 `/config`에서 **Claude-drafted feedback**을 `off`로.
> (2026-08-27 추가)

### `/permissions` — 권한 규칙을 화면에서 고치기 🆕
"이건 물어보지 말고 그냥 해라 / 이건 무조건 막아라" 규칙을 **설정 파일 안 열고** 화면에서 켜고 끕니다.
새로 생긴 **Auto mode 탭**에서 자동 판단 규칙을 바로 보고 수정할 수 있습니다.
```
/permissions     ← 열고 Auto mode 탭 선택
```
> 덤: 명령 실행 확인창이 떴을 때 **"Yes, and switch to auto mode"**를 고르면
> 그 명령을 승인하는 동시에 **이 세션을 자동 모드로 전환**합니다.
> 매번 확인창 누르기 지겨울 때, 확인창 안에서 바로 탈출하는 길입니다.
> (2026-08-28 추가)

### `/compact` — 대화 압축
⚠️ **핵심 팁**: 대부분 느려진 *다음에* 씁니다. 그건 이미 늦은 겁니다.
한 작업이 끝났고 아직 할 일이 남았을 때 **미리** 돌리는 게 정답입니다.
```
/compact
```

### `/config key=value` — 설정 한 줄 변경
메뉴 안 들어가고 즉시 바꿉니다. `-p` 모드와 Remote Control에서도 됩니다.
```
/config key=value
```
> 🆕 그냥 `/config`로 연 설정 화면은 이제 **마우스로도** 됩니다.
> 휠로 목록을 굴리고, 바꾸고 싶은 항목 값을 그냥 클릭하면 바뀝니다. (2026-09-14 추가, 전체화면 모드)

### `modelPicker` — 모델 고르는 목록 정리하기 🆕
`/model`을 치면 뜨는 **모델 후보 목록을 내가 정할 수 있습니다.**
안 쓰는 모델이 섞여 있어 매번 헷갈린다면, 쓰는 것만 남겨두세요.
```
/config modelPicker=opus,sonnet
```
> (2026-08-28 추가. 잘 안 알려진 설정입니다)

### `/theme` — 커스텀 색상 테마
직접 색 팔레트를 만들어 적용하거나, 플러그인으로 배포할 수 있습니다.
```
/theme
```

---

## 2. 진짜 "숨겨진" 것들

### `/radio` — Claude FM 로파이 라디오 🎵
작업 BGM. 진짜로 있습니다.
```
/radio
```

### `/powerup` — 클로드가 직접 가르쳐주는 강의
인터랙티브 레슨 형식으로 기능을 배웁니다. 기능 익히는 제일 빠른 길.
```
/powerup
```

### `--safe-mode` — 범인 색출 모드
스킬·플러그인 등 **커스터마이징을 전부 끄고** 실행합니다.
"뭐 때문에 이상한지 모르겠을 때" 이걸로 켜보면 원인이 커스터마이징인지 바로 압니다.
```bash
claude --safe-mode
```

### `~/.claude/` 폴더 — 모든 설정의 본진
숨김 폴더라 안 보이지만, **스킬·슬래시명령어·에이전트·CLAUDE.md**가 전부 여기 있습니다.
- 맥: `Cmd + Shift + G` → `~/.claude` 입력 → 엔터
- VS Code: File → Add Folder to Workspace → `~/.claude`

### 웹·앱에서 켠 스킬이 터미널에도 그대로 따라옵니다 🆕
그동안은 **claude.ai(웹/앱)에서 켜둔 스킬·플러그인**과
**터미널에서 쓰는 스킬**이 따로 놀아서, 같은 걸 양쪽에 각각 챙겨야 했습니다.
이제 같은 계정으로 로그인한 터미널 세션에는 **웹에서 켜둔 스킬·플러그인이 자동으로 따라옵니다.**
대본 스킬을 폰에서 켜두고 컴퓨터 터미널을 열면 거기서도 바로 잡힙니다.
- 따라오는 게 싫으면 끌 수 있습니다:
```
/config syncClaudeAiSkills=false
/config syncClaudeAiPlugins=false
```
> 주의: 동기화된 스킬 폴더의 파일을 터미널에서 고쳐도 **내 계정에는 저장되지 않습니다.**
> (그럴 땐 클로드가 저장하는 방법을 알려줍니다.)
> (2026-09-17 추가)

### 셸 모드 `!` — 명령 결과에 바로 반응
`!` 로 시작하면 셸 명령이 실행되는데, **결과를 클로드가 알아서 설명**해줍니다. 두 번 물어볼 필요 없습니다.
```
! npm test
```

### 문장 중간에 `/` 쳐서 명령어 고르기 🆕
예전엔 `/`를 **맨 앞에서** 쳐야 명령어가 떴고, 문장 중간에 치면 추천이 하나만 겨우 나왔습니다.
이제는 쓰던 문장 중간에 `/`를 쳐도 **조건에 맞는 명령어 목록이 쫙 펼쳐집니다.**
플러그인에 딸린 스킬도 앞에 플러그인 이름을 안 붙이고 스킬 이름만 쳐도 잡힙니다.
```
이 대본 팩트체크 먼저 하고 /econ    ← 여기서 목록이 뜸
```
> (2026-09-11 추가)

### 보낸 메시지 되가져오기 (웹) 🆕
클로드가 일하는 중에 메시지를 보내면 **대기열에 줄을 서는데**,
클로드가 아직 안 읽었다면 **Esc 또는 ↑(위 화살표)** 로 도로 빼올 수 있습니다.
글자는 입력창으로 그대로 돌아오니 고쳐서 다시 보내면 됩니다.
> (2026-09-11 추가. claude.ai/code 웹 세션)

### 대기 중인 메시지를 "지금 당장" 보내기 🆕
반대로, 클로드가 딴 길로 새고 있어서 **지금 바로 말을 끊고 싶을 때**가 있습니다.
이제 단축키 하나로 **하던 작업을 멈추고, 줄 서 있던 메시지를 한꺼번에** 밀어넣습니다.
```
Ctrl + Enter        (안 먹히는 터미널이면 Ctrl+X 다음 Ctrl+S)
```
보낸 메시지와 대기 중인 메시지는 클로드가 실제로 읽기 전까지 **회색으로** 표시되니,
어디까지 전달됐는지 눈으로 구분됩니다.
> (2026-09-17 추가)

### 한국어 추천 프롬프트 🆕
띄어쓰기 없이 붙여 쓴 한국어·일본어·중국어 문장에서 **추천 프롬프트가 안 뜨던 문제**가 고쳐졌습니다.
한국어로 길게 쓰는 대본 작업에서도 이제 제안이 정상적으로 나옵니다.
> (2026-09-11 반영)

---

## 3. MCP 관련

### `claude mcp login` / `logout`
`/mcp` 메뉴 안 들어가고 **터미널에서 바로** MCP 서버 인증을 처리합니다.
```bash
claude mcp login <서버이름>
claude mcp logout <서버이름>
```

### 🆕 MCP 서버가 끊기면 이제 알려줍니다
작업 중 노션·깃허브 같은 연결이 끊어지면 예전엔 **조용히 재접속을 시도하다 포기**해서,
왜 갑자기 "노션에 못 올린다"고 하는지 알 수가 없었습니다.
이제는 재접속을 포기한 순간 **알림이 뜨고 `/mcp`로 안내**해줍니다. 거기서 다시 연결하면 됩니다.
```
/mcp
```
> (2026-09-15 반영)

### MCP가 뭔데?
클로드를 **외부 서비스에 연결**하는 규격입니다. 붙여두면 그냥 말로 시킬 수 있습니다.
- Notion MCP → "노션에 이 대본 정리해서 올려줘"
- GitHub MCP → "이 변경사항 PR 올려줘"
- 데이터베이스 MCP → "이번 달 매출 뽑아줘"

---

## 4. 자동화 (가장 실용적)

### Routines (루틴)
스케줄·GitHub 이벤트·API 호출로 **클로드를 자동 실행**시킵니다.
웹의 **claude.ai/code → Routines**에서 관리하거나, 그냥 클로드한테 말로 시켜도 됩니다.
> "매주 월요일 아침에 유튜브 트렌드 조사해서 알려줘"

**🆕 Routines 화면이 새로 바뀌었습니다 (2026-09-14)**
- **Yours** 탭: 내가 만든 루틴. 카드마다 **지난 실행이 성공했는지 실패했는지** 바로 보입니다.
- **Templates** 탭: 미리 만들어둔 루틴 견본. 처음 만들 때 여기서 고르면 빈 화면부터 시작 안 해도 됩니다.
- 예전 달력(캘린더) 보기는 없어졌습니다.

**🆕 루틴 상세 화면도 쓰기 편해졌습니다 (2026-09-15)**
- **켜기/끄기 스위치와 [Run now](지금 실행) 버튼이 맨 위**로 올라왔습니다. 스크롤 안 해도 바로 눌러 테스트 가능.
- 이름 바꾸기·메뉴가 위쪽 경로 표시줄(빵가루 메뉴)에 붙었습니다.
- **실행 이력이 설정 옆에 나란히** 보여서, 어떤 설정으로 돌렸을 때 결과가 어땠는지 같이 봅니다.
- 루틴 이름·프롬프트를 쓰다가 창을 닫으면 **"저장 안 한 내용 버릴까요?" 확인창**이 뜹니다.
  (예전엔 길게 쓴 프롬프트가 그냥 날아갔습니다.)

**🆕 한도에 걸려도 작업을 안 버립니다 (2026-09-14)**
여러 작업을 한꺼번에 돌리다 사용량 한도에 걸리면 예전엔 그 작업들이 그냥 날아갔는데,
이제는 **멈춰서 기다렸다가 한도가 풀리면 알아서 이어서** 합니다. 처음부터 다시 시킬 필요가 없습니다.

**🆕 루틴이 결과 페이지를 알아서 갱신합니다 (2026-09-17)**
예약해둔 루틴이 만든 **공유 페이지(아티팩트)**를 다음 실행 때 새 내용으로 덮어쓰는데,
예전엔 "덮어써도 될까요?" 확인을 기다리다 **아무도 없는 새벽에 멈춰** 있곤 했습니다.
이제 **내가 편집 권한을 가진 페이지**라면 묻지 않고 갱신합니다.
- 단, **전체 공개 페이지·처음 만드는 페이지·삭제**는 여전히 물어봅니다.
- 활용: "매일 아침 트렌드 정리해서 같은 링크에 갱신해줘" → 링크 하나만 즐겨찾기 해두면 끝.

**🆕 루틴이 혼자 꺼져 있는 사고 방지 (2026-09-17)**
깃허브 연결이 잠깐 끊겼다는 이유로 루틴이 **첫 실패에 그냥 꺼져버리던** 문제가 고쳐졌습니다.
이제는 그 회차만 건너뛰고 **최대 72시간 동안 다시 시도**합니다.
또, 구독이 일시정지돼 루틴이 멈춘 경우 "알아서 다시 켜진다"고 잘못 안내하던 문구도
**"직접 다시 켜주세요"**로 정확하게 바뀌었습니다.

### 📱 모바일 푸시 알림
긴 작업이 끝나거나 클로드가 확인을 요청하면 **폰으로 알림**이 옵니다.
대본 뽑아놓고 딴짓해도 됩니다.

### `@` 멘션 — 세션끼리 대화
프롬프트에 `@`를 치면 **다른 클로드 세션을 이름으로 호출**합니다.
한 세션의 결과를 다른 세션에 설명 없이 넘길 수 있습니다.

### 🆕 폰에서 보던 세션을 내 컴퓨터로 "복제해서 떠넘기기"
`claude --remote-control`(또는 세션 안에서 `/remote-control`)로 폰과 연결해둔 세션을,
이제 **클로드 앱에서 바로 포크(복제)** 할 수 있습니다.
복제된 쪽은 **내 컴퓨터에서 백그라운드 세션으로** 돌아가니,
"지금 흐름은 그대로 두고, 곁가지 하나만 컴퓨터에서 오래 돌려놔" 같은 게 폰만 들고도 됩니다.
> (2026-09-15 추가)

### `claude agents` — 세션 관제탑
돌아가는 **모든 세션을 한 화면**에서 봅니다. 뭐가 실행 중이고, 뭐가 내 확인을 기다리고, 뭐가 끝났는지.
```bash
claude agents
```

### 📄 마크다운을 그대로 "문서 웹페이지"로 발행 🆕
대본·기획안처럼 클로드가 만들어준 **마크다운(.md) 파일을 아티팩트로 발행**하면,
이제 제목·본문·소제목 서식이 잡힌 **읽기 좋은 문서 페이지**로 나옵니다.
예전처럼 글자가 뭉쳐 나오지 않으니, 링크만 던져주면 외주 편집자·팀원이 바로 읽을 수 있습니다.
> "이 대본 링크로 공유할 수 있게 발행해줘"
> (2026-09-14 반영)

### Monitor 도구
로그 파일이나 프로세스를 **실시간으로 감시**하다가 변화가 생기면 즉시 반응합니다.
(주기적으로 확인하는 `/loop`와 달리, 사건이 터지는 순간 반응)

---

## 5. 2026년 신기능 하이라이트

| 시기 | 기능 | 한 줄 설명 |
|---|---|---|
| 2026-08 | `/usage` Loops 분해 | 루틴·반복 작업이 한도를 얼마나 먹었는지 1회당 토큰까지 |
| 2026-08 | 데스크톱 `/resume` | 터미널에서 하던 세션을 앱에서 맥락 그대로 이어받기 |
| 2026-08 | 클로드가 써주는 피드백 | 문제 생기면 신고서 초안을 대신 작성, 보낼지는 내가 결정 |
| 2026-08 | `/permissions` Auto mode 탭 | 권한·자동판단 규칙을 설정 파일 없이 화면에서 수정 |
| 2026-09 | 계정 스킬·플러그인 동기화 | 웹·앱에서 켜둔 스킬이 터미널 세션에도 자동으로 따라옴 |
| 2026-09 | 지금 보내기 단축키 | `Ctrl+Enter`로 작업을 끊고 대기 중인 메시지를 즉시 전달 |
| 2026-09 | 루틴의 페이지 자동 갱신 | 예약 실행이 내 공유 페이지를 확인 없이 갱신 |
| 2026-09 | 루틴 72시간 재시도 | 깃허브 연결 문제로 루틴이 혼자 꺼지지 않음 |
| 2026-09 | `/goal` 유지 수정 | 대화가 압축되거나 세션을 이어받아도 목표가 안 풀림 |
| 2026-08 | `modelPicker` | `/model` 목록에 띄울 모델을 내가 지정 |
| 2026-09 | 컴퓨터 사용 백그라운드 (데스크톱) | 승인한 앱만 뒤에서 조작 — 내 화면은 계속 내가 씀 (Pro·Max 베타, 맥) |
| 2026-09 | 데스크톱 패널 떼어내기 | 터미널·변경사항 패널을 별도 창으로 빼서 두 번째 모니터에 |
| 2026-09 | 문장 중간 `/` 명령어 목록 | 쓰던 문장 중간에 `/`를 쳐도 명령어 목록이 펼쳐짐 |
| 2026-09 | 루틴 상세 화면 개편 | 스위치·Run now가 맨 위 + 저장 안 한 프롬프트 보호 |
| 2026-09 | 폰 세션 → 내 컴퓨터로 포크 | 원격 제어 세션을 복제해 내 PC 백그라운드에서 돌림 |
| 2026-09 | MCP 끊김 알림 | 연결이 끊겨 재접속 포기하면 알림 + `/mcp` 안내 |
| 2026-09 | `/fast` 클라우드 지원 | 웹·루틴 세션에서도 빠른 모드 |
| 2026-09 | 마크다운 아티팩트 | .md 파일이 서식 잡힌 문서 페이지로 발행됨 |
| 2026-09 | Routines 개편 | Yours/Templates 탭 + 실행 성공·실패 표시 |
| 2026-09 | 한도 자동 재개 | 한도에 걸린 대량 작업이 리셋 후 알아서 이어짐 |
| 2026-09 | **Claude Fable 5.1** | 새 Fable 기본 모델. 100만 토큰, 캐시 읽기 비용 75% 인하 |
| 2026-09 | `/focus` | 중간 과정 숨기고 질문·요약·답변만 보는 조용한 화면 |
| 2026-09 | `/skill-doctor` | 안 쓰는 스킬과 각 스킬의 컨텍스트 비용 리포트 |
| 2026-09 | `/output-style` | 폰·웹·리모트에서도 출력 스타일을 이름으로 전환 |
| 2026-09 | `/diff` 패널 | 작업 중 바뀌는 파일을 대화 옆에 실시간으로 띄움 |
| W34 | `/design` | 아트보드 방식으로 UI 디자인 초안을 그려주고, 고른 걸 실제로 구현 (리서치 프리뷰) |
| W34 | Concise 출력 스타일 | 서론 빼고 **결론부터** 말하게 하기 |
| W34 | `ANTHROPIC_DEFAULT_MODEL` | 새 세션이 시작할 모델을 고정 |
| W33 | fork mode 기본 켜짐 | 곁가지 작업을 서브에이전트에게 넘기되, 전체 대화 맥락을 그대로 물려줌 |
| W32 | 세션 간 메시징 | macOS/Linux에서 세션끼리 정보 전달 |
| W30 | **Claude Opus 5** | 현재 기본 모델. **100만 토큰** 컨텍스트 |
| W30 | Claude Security 플러그인 | 멀티 에이전트 취약점 스캔 |
| W29 | Artifacts + MCP | 발행한 아티팩트가 **보는 사람의 MCP로 실시간 데이터**를 끌어옴 |
| W29 | 스크린 리더 모드 | VoiceOver·NVDA용 평문 인터페이스 |
| W27 | **Claude Sonnet 5** | 100만 토큰 + 적응형 사고 기본 탑재 |
| W27 | Claude in Chrome | 정식 출시 |
| W25 | **Artifacts** | 세션 결과물을 **공유 가능한 웹페이지**로. 작업하면서 실시간 갱신 |
| W21 | `/code-review` | 코드 정확성 버그 자동 검출 |
| W17 | `/ultrareview` | 클라우드에서 버그 사냥 에이전트 부대를 돌리고 결과만 받아봄 |
| W16 | **Routines + 푸시 알림** | 스케줄 자동 실행 + 폰 알림 |
| W13 | **Auto mode** | 권한 확인창을 분류기가 대신 처리 — 안전한 건 통과, 위험한 건 차단 |

---

## 6. 출력 스타일 — 클로드의 말투·태도 바꾸기

출력 스타일은 클로드가 **무엇을 아는지가 아니라 어떻게 답하는지**를 바꿉니다.
매번 "짧게 말해", "묻지 말고 그냥 해"라고 반복하고 있다면 스타일로 박아두세요.

기본 제공 스타일 5종:

| 스타일 | 성격 |
|---|---|
| Default | 기본값 (개발 작업 기준) |
| **Proactive** | 사소한 건 안 묻고 **알아서 판단하고 바로 실행**. 계획보다 행동 우선 |
| **Concise** | 서론·중계방송 없이 **결론부터**, 짧게 |
| Explanatory | 작업하면서 "왜 이렇게 했는지" 해설을 곁들임 |
| Learning | 일부러 빈칸을 남겨두고 사용자가 채우게 하는 학습 모드 |

바꾸는 법:
```
/config          ← 터미널: 메뉴에서 Output style 선택
/output-style Concise   ← 폰·웹·리모트 세션: 이름으로 바로 전환 (2026-09-11 추가)
```

나만의 스타일도 만들 수 있습니다. `~/.claude/output-styles/` 폴더에 마크다운 파일을 하나 만들고
맨 위에 이름·설명을 적은 뒤, 원하는 말투 규칙을 그냥 한국어로 써두면 됩니다.
(개발 작업이 아니라면 `keep-coding-instructions`는 넣지 마세요.)

---

## 7. 데스크톱 앱 (Mac / Windows) 🆕

터미널 대신 **클로드 코드 데스크톱 앱**을 쓸 때만 되는 것들입니다.

### 🖥️ 컴퓨터를 클로드에게 맡기기 — 이제 "백그라운드"로
클로드가 내 컴퓨터의 앱을 직접 보고 클릭하는 기능인데,
예전에는 **화면 전체를 넘겨줘야** 해서 클로드가 일하는 동안 나는 손을 놓고 기다려야 했습니다.
이제 맥에서는 **내가 승인한 앱에서만 뒤에서 조용히** 움직이므로,
클로드가 그 앱을 만지는 동안 나는 딴 창에서 계속 내 일을 할 수 있습니다.
- 켜는 법: 클로드가 앱을 쓰겠다고 요청할 때 뜨는 **Computer use 카드에서 `Enable`**
- **Pro·Max 플랜 베타, macOS 전용**
> (2026-09-04 반영)

### 🔄 터미널에서 하던 작업을 앱에서 그대로 이어받기 🆕
터미널(CLI)에서 시작한 세션을 **데스크톱 앱에서 이어서** 할 수 있습니다.
대화 내용과 맥락이 **그대로** 넘어오므로, 앞에서 설명한 걸 다시 설명할 필요가 없습니다.
```
/resume
```
제목·폴더·브랜치로 검색할 수 있고, 고르기 전에 **어디까지 했는지 미리보기**가 뜹니다.
터미널에서 대본 뽑다가 "이제 편한 화면에서 보고 싶다" 할 때 쓰면 됩니다.
> (2026-08-24 추가)

### 🪟 패널을 별도 창으로 떼어내기
데스크톱 앱의 패널(터미널, 변경사항 화면 등)을 **창 밖으로 끌어내** 독립 창으로 띄울 수 있습니다.
듀얼 모니터면 결과 화면을 옆 모니터에 걸어두고 본 창에서는 대화만 하면 됩니다.
다 보고 나면 다시 끌어다 원래 자리에 붙이면(dock) 됩니다.
- 하는 법: 패널 제목 부분을 **잡고 창 밖으로 드래그**
> (2026-09-11 추가)
